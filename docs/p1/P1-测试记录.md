# P1 测试记录

## P1.2 AdapterProtocol 固化

### 测试环境

| 项目 | 值 |
|------|-----|
| 测试时间 | 2026-05-06 |
| 分支 | dev_p0 |
| Node.js | v22.15.0 |

### 测试执行

| 测试文件 | 用例数 | 结果 |
|---------|--------|------|
| `tests/ide/adapter-protocol.test.js` | 24 | 通过 |
| `tests/ide/cursor-adapter.test.js` | 7 | 通过 |
| `tests/ide/claude-adapter.test.js` | 7 | 通过 |
| `tests/ide/ide-sync.test.js` | - | 通过 |
| `tests/scanner/tech-scanner.test.js` | - | 通过 |
| `tests/registry/validate-registry.test.js` | - | 通过 |

### 关键验证点

1. IDEAdapter 基类 adapterId 抛出错误要求子类实现
2. createAdapterInput 默认值正确（profile: 'auto'）
3. createAdapterOutput 返回标准结构（adapterId, files, warnings, generatedAt）
4. createValidationResult 区分 error/warning severity
5. CodexAdapter 返回 applicable: false 且 files 为空
6. validateAdapterConsistency 检查 spec-start 覆盖、入口文件、类型多样性
7. CursorAdapter 生成 9 个文件（6 rules + 3 commands）
8. ClaudeAdapter 生成 12 个文件（1 entry + 6 commands + 4 agents + 1 config）
9. validate/diff 支持 profile 参数传递
10. rollback 正确删除已写入的文件

## P1.3 AssetPackage 标准化

### 测试环境

| 项目 | 值 |
|------|-----|
| 测试时间 | 2026-05-06 |
| 分支 | dev_p0 |
| Node.js | v22.15.0 |

### 测试执行

| 测试文件 | 用例数 | 结果 |
|---------|--------|------|
| `tests/asset/asset-package.test.js` | 35 | 通过 |
| `tests/ide/adapter-protocol.test.js` | 24 | 通过 |
| `tests/ide/cursor-adapter.test.js` | 7 | 通过 |
| `tests/ide/claude-adapter.test.js` | 7 | 通过 |
| `tests/ide/ide-sync.test.js` | - | 通过 |
| `tests/scanner/tech-scanner.test.js` | - | 通过 |
| `tests/registry/validate-registry.test.js` | - | 通过 |

### 关键验证点

1. ASSET_TYPES 包含 9 种类型（rule/skill/agentProfile/command/hook/memory/config/adapter/other）
2. ASSET_SOURCES 包含 3 种来源（local/hub/template）
3. createAssetPackage 默认值正确（assetType: 'other', version: '0.1.0', source: 'local'）
4. validateAssetPackage 校验 assetId/assetType/version/source/checksum/generatedFiles
5. guessAssetType 从路径正确推断资产类型
6. computeAssetChecksum 基于内容计算 sha256 摘要
7. buildAssetIdentity 生成 type:id@version 格式标识
8. install 成功写入文件，失败时回滚已写入文件
9. upgrade 备份旧版本后写入新版本，保留备份元数据
10. rollback 从备份恢复文件，删除新增文件
11. verifyChecksum 校验文件是否存在且可读
12. verifyFileChecksum 校验单文件 checksum 一致性
13. getGeneratedFiles 返回文件列表及存在状态
14. cleanupGeneratedFiles 删除所有生成文件
15. listBackups 列出指定资产的所有备份

## P1.4 ConfigLayer 配置分层

### 测试环境

| 项目 | 值 |
|------|-----|
| 测试时间 | 2026-05-06 |
| 分支 | dev_p0 |
| Node.js | v22.15.0 |

### 测试执行

| 测试文件 | 用例数 | 结果 |
|---------|--------|------|
| `tests/config/config-layer.test.js` | 42 | 通过 |
| `tests/asset/asset-package.test.js` | 35 | 通过 |
| `tests/ide/adapter-protocol.test.js` | 24 | 通过 |
| `tests/ide/cursor-adapter.test.js` | 7 | 通过 |
| `tests/ide/claude-adapter.test.js` | 7 | 通过 |

### 关键验证点

1. CONFIG_LAYERS 包含 11 层（default/enterprise/global/manifest/agentProfile/workspace/project/team/policy/run/cli）
2. 层优先级严格递增，priority 值 0→10
3. LayerRegistry 按优先级排序返回所有层
4. mergeConfigs 按层顺序合并，高优先级覆盖低优先级
5. 深层对象递归合并，数组整体替换
6. null 层被安全跳过不影响合并
7. detectConflicts 检测多层设置同一字段且值不同的情况
8. 相同值不触发冲突检测
9. 本地覆盖正确：project 优先于 workspace，cli 优先于所有层
10. READONLY_FIELDS 包含 6 个隐私策略字段
11. checkReadonlyFields 仅在字段被显式设置且值不同时触发违规
12. forcePrivacyPolicy 强制覆盖隐私策略字段为 false
13. loadEnterpriseConfig 从 .ai-spec/enterprise.json 或全局路径加载
14. loadTeamConfig 从 .ai-spec/team.json 加载
15. 文件不存在时返回 null，JSON 解析失败时抛出带 code 的错误

## P1.5 多项目接入验证

### 测试环境

| 项目 | 值 |
|------|-----|
| 测试时间 | 2026-05-06 |
| 分支 | dev_p0 |
| Node.js | v22.15.0 |

### 测试执行

| 测试文件 | 用例数 | 结果 |
|---------|--------|------|
| `tests/p1/multi-project-validation.test.js` | 12 | 通过 |
| `tests/config/config-layer.test.js` | 42 | 通过 |
| `tests/asset/asset-package.test.js` | 35 | 通过 |
| `tests/ide/adapter-protocol.test.js` | 24 | 通过 |
| `tests/ide/cursor-adapter.test.js` | 7 | 通过 |
| `tests/ide/claude-adapter.test.js` | 7 | 通过 |
| `tests/scanner/tech-scanner.test.js` | - | 通过 |
| `tests/ide/ide-sync.test.js` | - | 通过 |
| `tests/registry/validate-registry.test.js` | - | 通过 |

### 关键验证点

1. React (Vite) 项目被正确检测为 react-vite 框架，置信度 >= 80
2. Vue (Vite) 项目被正确检测为 vue-vite 框架，置信度 >= 80
3. NestJS 项目被正确检测为 nestjs 框架，置信度 >= 60
4. CursorAdapter 对三类项目均生成 >= 6 个文件
5. ClaudeAdapter 对三类项目均生成 >= 10 个文件
6. profile='auto' 时三类项目仍能正确生成文件
7. 跨适配器一致性校验（validateAdapterConsistency）对三类项目均通过
8. 兼容矩阵覆盖 3 种项目类型，所有项目检测框架非 unknown
9. 全量回归测试（9 个测试文件）全部通过

## P1.6 P1 集成回归与阶段验收

### 测试环境

| 项目 | 值 |
|------|-----|
| 测试时间 | 2026-05-06 |
| 分支 | dev_p0 |
| Node.js | v22.15.0 |

### 测试执行

| 测试文件 | 用例数 | 结果 |
|---------|--------|------|
| `tests/p1/p1-integration.test.js` | 10 | 通过 |
| `tests/p1/multi-project-validation.test.js` | 12 | 通过 |
| `tests/config/config-layer.test.js` | 42 | 通过 |
| `tests/asset/asset-package.test.js` | 35 | 通过 |
| `tests/ide/adapter-protocol.test.js` | 24 | 通过 |
| `tests/ide/cursor-adapter.test.js` | 7 | 通过 |
| `tests/ide/claude-adapter.test.js` | 7 | 通过 |
| `tests/scanner/tech-scanner.test.js` | - | 通过 |
| `tests/ide/ide-sync.test.js` | - | 通过 |
| `tests/registry/validate-registry.test.js` | - | 通过 |

### 关键验证点

**集成测试 (TC01)**
1. 全链路：Scanner 检测 → Adapter 生成 → 一致性校验 → AssetPackage 创建/安装 → ConfigLayer 合并

**回归测试 (TC02)**
2. Scanner 回归：Vue 项目仍被正确检测为 vue-vite
3. Adapter 回归：CursorAdapter 生成 >= 6 文件，ClaudeAdapter 生成 >= 10 文件
4. AssetPackage 回归：install/upgrade/rollback 全流程正常
5. ConfigLayer 回归：11 层顺序、合并、冲突检测、只读保护均正常
6. ConfigLoader 回归：隐私策略强制覆盖正常

**回滚测试 (TC03)**
7. Asset 回滚：升级后回滚恢复旧文件、删除新增文件
8. Adapter 回滚：rollback 正确删除所有已写入文件

**接口稳定性 (TC04)**
9. 所有 P1 模块公共接口存在且类型正确
10. 重复安装相同内容具备幂等性

## P1.7 工程化门禁与 AssetPackage checksum 语义加固

### 测试环境

| 项目 | 值 |
|------|-----|
| 测试时间 | 2026-05-06 |
| 分支 | dev_p0 |
| Node.js | v22.15.0 |

### 测试命令

| 命令 | 结果 |
|------|------|
| `node tests/asset/asset-package.test.js` | 通过 |
| `npm run test:p1` | 通过 |
| `npm run verify:p1` | 通过 |

### 测试执行

| 测试文件 | 用例数 | 结果 |
|---------|--------|------|
| `tests/asset/asset-package.test.js` | 40（含 5 个新增） | 通过 |
| `tests/ide/adapter-protocol.test.js` | 24 | 通过 |
| `tests/ide/cursor-adapter.test.js` | 7 | 通过 |
| `tests/ide/claude-adapter.test.js` | 7 | 通过 |
| `tests/config/config-layer.test.js` | 42 | 通过 |
| `tests/p1/multi-project-validation.test.js` | 12 | 通过 |
| `tests/p1/p1-integration.test.js` | 10 | 通过 |
| `tests/scanner/tech-scanner.test.js` | - | 通过 |
| `tests/ide/ide-sync.test.js` | - | 通过 |
| `tests/registry/validate-registry.test.js` | - | 通过 |

### 工程门禁验证

| 脚本 | 命令 | 结果 |
|------|------|------|
| `test:p1` | 9 个测试文件串行执行 | 通过 |
| `verify:p1` | test:registry + test:runtime + test:p1 + npm pack --dry-run | 通过 |

### 关键验证点

1. `npm run test:p1` 可执行且全部通过
2. `npm run verify:p1` 可执行且全部通过
3. `verifyChecksum` 应在包级 checksum 一致时通过
4. `verifyChecksum` 应在文件内容被篡改时失败（返回 expected/actual）
5. `verifyChecksum` 应在 generatedFiles 文件缺失时失败
6. `verifyChecksum` 应支持多个 generatedFiles 的稳定 checksum（排序后一致）
7. `verifyFileChecksum` 应保持单文件校验能力
8. 不影响已有 test:registry、test:runtime、prepack
