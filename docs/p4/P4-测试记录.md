# P4 测试记录

## 测试环境

| 项目 | 值 |
|------|------|
| 运行时 | Node.js |
| 测试框架 | assert + async main() |
| 测试命令 | `npm run test:p4`、`npm run verify:p4` |

## 测试结果汇总

| 测试文件 | 用例数 | 通过 | 失败 | 状态 |
|----------|--------|------|------|------|
| `tests/visual/event-gateway.test.js` | 40 | 40 | 0 | 通过 |
| `tests/visual/timeline.test.js` | 21 | 21 | 0 | 通过 |
| `tests/visual/hook-dashboard.test.js` | 20 | 20 | 0 | 通过 |
| `tests/visual/agent-visual.test.js` | 21 | 21 | 0 | 通过 |
| `tests/visual/metrics.test.js` | 23 | 23 | 0 | 通过 |
| `tests/visual/risk-board.test.js` | 30 | 30 | 0 | 通过 |
| `tests/p4/p4-integration.test.js` | 24 | 24 | 0 | 通过 |
| **合计** | **179** | **179** | **0** | **全部通过** |

## P4.1 Event Gateway（40 用例，含 P4.8 新增 9 用例）

- Schema 校验（必填字段缺失、非法枚举值、类型错误、null/空对象）
- 红脱（密码消息、apiKey 元数据、无消息元数据）
- 事件 ingest（正常写入、eventId 自增、时间戳生成、校验失败）
- NDJSON 持久化（写入文件、重启恢复、坏行容错）
- 查询（按 eventType、severity、runId、时间范围、limit）
- 统计与导出（JSON、NDJSON）
- 清空与 size（clear、eventId 重置）
- 幂等性与工厂函数
- **P4.8 新增**：坏行 loadErrors 记录、getLoadErrors 副本、写入失败 writeErrors 记录、getWriteErrors 副本
- **P4.8 新增**：throwOnWriteError=true 抛错、clear 默认只清内存、clear({ clearFile: true }) 清空文件
- **P4.8 新增**：写入失败不影响内存查询、坏行不影响正常事件恢复

## P4.2 Run Timeline（21 用例）

- 阶段排序验证（7 阶段固定顺序）
- 按阶段分组（正常/空事件）
- 按事件类型分组
- 阶段详情（耗时计算、失败原因提取、不存在阶段、无事件阶段）
- 摘要（正确统计、零失败）
- 空事件列表处理（空数组、null 输入）
- 序列化（toJSON 可序列化、不含 events 详情）
- resolveStageStatus（全成功/有 failed/有 blocked）
- extractFailureReason（有失败/无失败）
- runId 推断、工厂函数

## P4.3 Hook/Test/Repair 可视化（20 用例）

- Hook 结果分析（通过/失败/跳过统计、失败原因提取、分组统计、平均耗时、过滤非 Hook）
- Test 结果分析（通过率、失败列表、全部通过、空数据、null 输入）
- Repair 结果分析（成功率、平均修复次数、空数据、失败原因）
- 失败摘要（原因分类、阶段分布、修复成功率、空数据）
- 工厂函数

## P4.4 Agent 协作可视化（21 用例）

- Agent 时间线（构建列表、开始时间、escalated、blocked、多 Agent、toolCalls、过滤非 Agent、无 agentId）
- Handoff 视图（提取、空数组、多个 handoff）
- 冲突视图（tool_denied、file_scope、max_iterations、空数组）
- 人工门禁视图（提取、pending 默认值、空数组、null 输入）
- buildAgentTimeline 集成、工厂函数

## P4.5 质量指标与趋势分析（23 用例）

- 空数据（空数组、null、未 compute getter、toJSON 零值）
- taskSuccessRate（全成功、全失败、混合状态）
- firstPassRate（无 repair 直接成功、有 repair）
- repairSuccessRate（有 repair 成功、无 repair）
- hookFailureRate（正确计算、无 hook）
- ruleHitRate（正确计算、无 policy_denied）
- toJSON（包含所有字段）
- 趋势（daily 聚合、weekly 聚合、非法 window、空事件、hookFailureRate）
- 幂等性、工厂函数

## P4.6 风险与审计看板（30 用例，含 P4.8 新增 9 用例）

- 策略拒绝（提取、空数组）
- 安全风险（提取、空数组）
- 越权尝试（提取、agentId）
- 审计操作（提取、空数组）
- 风险摘要（正确计数、blocking→critical、error→high、>5 拒绝→high、warn→medium、无事件→low、computedAt）
- 多维过滤（severity、type、type 前缀、时间范围、null 输入）
- 工厂函数
- **P4.8 新增**：severity→riskLevel 映射（info/warn/error/blocking/low/medium/high/critical/未知降级）
- **P4.8 新增**：getRiskLevelRank 排序权重、topRisks 排序稳定性、getRiskSummary 统一 riskLevel 输出

## P4.7 集成回归（24 用例，含 P4.8 新增 TC16 的 5 个子用例）

- TC01: EventGateway 全链路（ingest→查询→统计→导出）
- TC02: RunTimeline 全链路（聚合→阶段排序→分组→摘要）
- TC03: HookDashboard 全链路（Hook/Test/Repair 分析→失败摘要）
- TC04: AgentVisual 全链路（Agent 时间线→Handoff→门禁）
- TC05: Metrics 全链路（指标计算→趋势）
- TC06: RiskBoard 全链路（风险识别→摘要→过滤）
- TC07: EventGateway + Timeline 联动
- TC08: Timeline + Dashboard 联动
- TC09: Dashboard + Metrics 联动
- TC10: RiskBoard + AuditLog 联动
- TC11: 接口稳定性
- TC12: 幂等性
- TC13: barrel 导出验证
- TC14: NDJSON 事件流可作为可视化数据源
- TC15: 隐私安全（自动红脱）
- **P4.8 新增** TC16: EventGateway 错误可见性与 RiskBoard 风险语义一致性
  - 读取含坏行 NDJSON 应记录 loadErrors
  - 写入失败时应记录 writeErrors
  - RiskBoard blocking→critical 映射
  - topRisks 排序稳定性
  - 正常事件不受坏行影响
- P1+P2+P3 回归（3 个模块加载验证）

## 验证命令

| 命令 | 结果 |
|------|------|
| `node ./tests/visual/event-gateway.test.js` | 40/40 通过 |
| `node ./tests/visual/risk-board.test.js` | 30/30 通过 |
| `node ./tests/p4/p4-integration.test.js` | 24/24 通过 |
| `npm run test:p4` | 179/179 通过 |
| `npm run verify:p4` | 全量通过（registry + runtime + p1 + p2 + p3 + p4 + dry-run） |
