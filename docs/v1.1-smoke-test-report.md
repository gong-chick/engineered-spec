# V1.1 Smoke Test 报告

## 服务健康检查

| 服务 | 地址 | 结果 |
| --- | --- | --- |
| Hub | `http://localhost:3000/api/hub/manifests/recommend` | HTTP 200，返回统一 ApiResponse |
| Visual Collector | `http://172.20.10.4:18780/api/collector/project-state` | HTTP 200，Project State 写入成功 |
| Visual 页面 | `http://172.20.10.4:18780/w/test_%E5%89%AF%E6%9C%AC17` | HTTP 307，跳转登录页，页面服务在线 |

## 临时项目

路径：

```text
/tmp/ai-spec-v1-1-smoke
```

结果：

| 命令 | 结果 |
| --- | --- |
| `scan --explain` | 通过，识别 `VueViteDetector`，置信度 95 |
| `scan --json` | 通过，workspace=`single-project` |
| `init --recommend --dry-run` | 通过，推荐来源 Hub，推荐 `frontend-vue-vite-standard`，不写文件 |
| `init --recommend --yes` | 通过，写入 `.ai-spec` / `.agents` / IDE 指针文件 |
| `sync` | 通过，资产 2 个，Agent Profile 1 个，checksum 通过 |
| `check` | 通过，错误 0、警告 0 |
| `guard assets` | 通过，警告 0 |
| `spec-start --dry-run` | 通过，创建 run，不创建 branch / worktree |
| `spec-status` | 通过 |
| `spec-continue --execute --dry-run` | 通过，进入 human_review，不调用真实执行器 |

隐私检查：

1. `registry.index.json` 不包含 `"content"`。
2. `run.json` 不包含 `/Users/`。
3. `run.json` 不包含 `/tmp/ai-spec-v1-1-smoke` 绝对路径。
4. `run.json` 不包含 `sourceCode`。

## 真实业务项目 Dry-run 回归

| 项目 | 工作区类型 | 识别结果 | 推荐来源 | 结果 |
| --- | --- | --- | --- | --- |
| `asset-cube` | `maven-multi-module` | Spring MVC / Spring Boot 多模块 | Hub | scan / json / init dry-run 通过 |
| `asset-cube-html` | `pnpm-workspace` | Vue Vite workspace | Hub | scan / json / init dry-run 通过 |
| `bulldog` | `single-project` | React Webpack | Hub | scan / json / init dry-run 通过 |
| `trace` | `multi-project-workspace` | React Vite + NestJS | Hub | scan / json / init dry-run 通过 |
| `asset-cube-workspace` | `multi-project-workspace` | Spring Cloud + Vue Vite | Hub | scan / json / init dry-run 通过 |

写入检查：

1. 未执行 `init --yes`。
2. Git 项目前后 `git status --short` 一致。
3. `trace` 和 `asset-cube-workspace` 非 Git 仓库，已记录。

## 普通测试项目 Dry-run 回归

| 项目 | 识别结果 | 推荐来源 | 结果 |
| --- | --- | --- | --- |
| `test_副本18` | Vue Vite | Hub | 通过 |
| `test_副本19` | Vue Vite | Hub | 通过 |
| `test_副本20` | Vue Vite | Hub | 通过 |
| `test_副本21` | Vue Vite | Hub | 通过 |

写入检查：

1. 未执行 `init --yes`。
2. 未写入 `.ai-spec`。
3. 未写入 `.agents`。

## 小问题修复

临时项目名 `ai-spec-v1-1-smoke` 包含 `spec`，此前会产生 `cli-tool` 误判 warning。已修复为：存在明确前端入口且 primary detector 置信度高时，不仅凭名称判定 CLI 工具。
