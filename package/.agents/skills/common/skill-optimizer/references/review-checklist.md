# Skill 审查清单

按以下顺序检查：

1. 触发是否清晰
2. 工作流是否可执行
3. 是否存在必须确认却没有卡住的步骤
4. 正文是否过胖，是否应拆到 `references/` 或 `scripts/`
5. 输出是否稳定、便于交接
6. 是否有明显的默认行为越权

优先修复：

- 触发不准
- 工作流断裂
- 缺少确认门槛

其次修复：

- 结构臃肿
- references / scripts / assets 组织混乱
- 输出格式不稳定
