# Node.js Tooling Profile Skills

此目录存放 Node.js Tooling profile 专属技能。

当前已规划（待实现）的技能：

- `create-command` — 按团队规范创建 CLI 命令及参数定义
- `create-contract` — 按团队规范创建 Contract / Schema 文件
- `create-worker` — 按团队规范创建 Worker 任务
